# 半導體分析 × Active Learning:還缺什麼功能(2026-07-19 思考)

> 前提:排除「已實作」與「已列候選」的項目,聚焦**還沒被任何清單載到的缺口**。
> 每項附:為什麼半導體/AL 需要 → 現有最近落點 → 單機最小版 → **客觀驗證法**
> (沿本 repo 的植入驗收文化:提案時就想好怎麼證明它真的有效,不做無法驗收的功能)。
> 相關文件:[08_平台概念差距分析](5_active_learning_product_review_2026-07-12/08_平台概念差距分析.md)(平台視角)、
> [FEATURE_CLARITY_REVIEW](FEATURE_CLARITY_REVIEW_2026-07-19.md)(現有功能盤點)。

---

## 0. 現況一句話地圖(避免重複發明)

| 能力 | 已有 |
|---|---|
| 探索/比較 | Visualize(框選/搜圖/離群/分歧)、Compare(兩堆漂移)、完整度(embedding 覆蓋) |
| 品質審計 | 🩺 資料體檢(六訊號+method line+metadata CSV 分組) |
| 瑕疵/差異 | 瑕疵偵測(單張 vs Normal Bank+閘控 Unknown)、🧪 差異探索(群對群+null 誠實判定) |
| 檢索/預標 | 🎯 以樣搜樣(免建模海撈+YOLO 預標)、找同款、heatmap→框 |
| AL 迴圈 | 分批引擎/池狀態機(永不重送)/回合帳本/混合選樣(FPS 去重)/代理評估/讀回落地 |
| 已列候選 | S1/S2/S3 統一語言、AL Promotion Gate、特徵版本治理、儲存 GC、兩資料夾距離、報告快照比較、immutable dataset version(待決) |

---

## 1. 半導體域特有的缺口(依價值排序)

### 1.1 晶圓空間視圖與空間模式(最大的域缺口)⭐

- **為什麼**:半導體影像幾乎都掛著空間座標(die x/y、wafer 上位置、lot/wafer id)。
  **缺陷的空間分佈本身就是根因線索**:edge ring=環邊製程、center cluster=旋塗/腔體、
  scratch line=機械傷、radial=旋轉不均——這些 pattern 工程師一眼能讀,但目前工具把每張
  影像當獨立樣本,空間語境完全丟失。全 repo 沒有任何 wafer-map 概念。
- **現有最近落點**:M20 metadata CSV 已能把任意欄位(含 x/y 座標)掛上影像——
  **管線已通,只差視圖**。分群/異常分數/類別都已算好,缺的是把它們畫回晶圓座標。
- **單機最小版**:metadata CSV 帶 `die_x`/`die_y`(+`wafer_id`)→ 新視圖:每 wafer 一張
  散點(座標=die,顏色=類別/異常分數/分群),多 wafer 並排;加一個「空間聚集度」指標
  (如最近鄰距離 vs 隨機分佈的 Clark-Evans / permutation 檢定——**沿 M19 的 null 誠實
  手法**:隨機打散座標當基準,聚集不顯著就明講)。
- **客觀驗證**:合成植入——隨機分佈 vs 植入 edge-ring/scratch pattern 的座標 →
  聚集指標顯著/不顯著判定正確;E2E 斷言植入 pattern 的 wafer 排在「最聚集」前列。
- **前置確認(需使用者拍板)**:你的資料是否有 die/wafer 座標欄位?沒有座標這項就不成立。

### 1.2 Tool/Recipe 差異顯著性檢定(把 M19 的誠實檢定帶進製程端)⭐

- **為什麼**:M20 的 metadata 分組現在只給**描述性計數**(T1 幾張、T2 幾張)。
  工程師真正要問的是:「**B 機台的瑕疵率比 A 高,是真的還是抽樣雜訊?**」——
  這是製程歸因的第一問,答錯就白查一台機。
- **現有最近落點**:M20 分組計數 + M19 的 permutation 檢定框架(現成的統計紀律);
  瑕疵偵測/分類頭已產出 per-image 判定可當「結果」欄。
- **單機最小版**:資料體檢加一節(或差異探索加分頁):選一個 metadata 欄位 ×
  一個結果欄位(瑕疵率/某類別佔比/異常分數)→ 分組率 + permutation p 值 +
  「未達顯著」誠實判定;多欄位時控多重比較(max-統計量,M19 同款)。
- **客觀驗證**:合成——同分佈分組 → 判無差異;植入 tool=B 瑕疵率 ×3 → 判 B 顯著且方向正確。

### 1.3 批次/時間軸與異變(excursion)偵測

- **為什麼**:影像是**隨時間到達的**(lot by lot)。「這週的資料跟上月比有沒有跑掉」
  「新 lot 是否冒出一群沒見過的東西」= excursion 偵測,是產線日常。現況 Compare 是
  一次性的 A/B 兩堆,M10 監看只累積佇列,**沒有時間軸**。
- **現有最近落點**:M10 監看服務(增量掃描已有)、Compare 漂移分數、M3/M6 Unknown 閘控、
  M20 metadata 的日期欄、候選中的「報告快照比較」是它的靜態子集。
- **單機最小版**:監看服務每輪落一筆時間戳摘要(瑕疵率/Unknown 率/形心漂移距離)→
  📥 或監看分頁畫趨勢線;「新群警示」= 本輪 Unknown 樣本對歷史全集做一次分群,
  成群(≥N 顆且彼此近)才警示——單顆離群不吵人。
- **客觀驗證**:合成時序資料——平穩段警示 0 次;植入「第 k 輪起出現新型樣」→
  第 k 輪警示、且指到正確的那群。

### 1.4 成本敏感工作點(escape ≠ overkill)

- **為什麼**:半導體的錯放(escape)與誤殺(overkill)成本差幾個數量級。
  現況閘控門檻/分數 slider 都是**對稱**調的,沒有地方輸入「escape:overkill = 100:1」
  然後給我建議工作點。這也是 Business Risk 治理(候選)最務實的第一塊拼圖。
- **現有最近落點**:瑕疵偵測的分數分佈+門檻校準、probe_eval 的 recall 曲線、
  體檢卡的 escape 概念。
- **單機最小版**:瑕疵偵測②加「工作點面板」:輸入兩個相對成本 → 在既有分數分佈上
  畫期望成本曲線 → 建議門檻 + 該點的 escape/overkill 數;**只建議不自動套**(沿
  「不自動停」的產品紀律)。
- **客觀驗證**:合成已知分佈 → 期望成本最小點的解析解 vs 面板建議一致;
  成本比 1:1 時退化為現行對稱行為。

### 1.5 瑕疵案例庫(defect library:把「處理過」沉澱下來)

- **為什麼**:工程師最常做的動作是「這個像不像上次那批?上次怎麼結案?」。
  以樣搜樣能找**像的影像**,但找不到**上次的結論**——案例(影像群+當時的判定+
  根因筆記+處置)沒有任何落地容器。08 文件的 Similar Case Search 缺的其實就是這層。
- **現有最近落點**:sample_bank(向量+類別已會存)、策展日誌(選了哪批+為什麼,
  已跨重啟保存!)、M20 metadata。三者各有一半。
- **單機最小版**:策展日誌條目升級為「案例」:可附一段結案筆記 + 標籤(根因分類);
  以樣搜樣結果旁多一欄「命中歷史案例:○○(相似度 x)→ 看筆記」。純 `.lv_cache`
  JSON,不需資料庫。
- **客觀驗證**:E2E——建案例→新資料海掃→植入的同型樣本確實連回該案例;
  刪案例→不再連(無 stale)。

---

## 2. Active Learning 迴圈的缺口(依價值排序)

### 2.1 群組批次標註(cluster-level confirm)——標註效率的最大剩餘槓桿 ⭐

- **為什麼**:現有迴圈把「選哪些」做得很好(混合選樣/去重/佇列),但**確認動作
  本身還是逐張點**。實務上佇列裡常是「一整群幾乎一樣的東西」——正確的人機分工是
  「**這 40 顆是同一群,全標 A,我抽 3 顆確認**」:一次動作標一群+例外剔除,
  標註量再降一個量級。M1 的 2-stage 分群確認就是這個思想,但只活在建模階段,
  沒進 AL 佇列。
- **現有最近落點**:bootstrap_cluster、佇列已有分群/多樣性資訊、readback_store
  可承接批次寫入。
- **單機最小版**:佇列加「依相似度成組顯示」開關:組卡片(代表圖+成員數)→
  「整組標 X」+ 點開剔除例外;寫回時逐顆落帳(帳本記 group_confirm 來源,
  抽查率可稽核——**誠實紀律:整組標註要留「是批次標的」痕跡**,後續評估才能
  區分逐張 vs 批次的品質)。
- **客觀驗證**:合成同質群+植入 2 顆異類 → 整組標註後例外剔除流程能揪出;
  帳本 audit 能算出「批次標註佔比」。

### 2.2 模型晉升閘門 + 訓練資料集版本釘選(策略 gate 的姊妹篇)

- **為什麼**:候選清單的 AL Promotion Gate 管「**選圖策略**該不該換」;同樣的問題
  存在於「**重訓後的 head/模型**該不該取代舊的」。現況訓完 head 就直接用,
  沒有新舊對照;而且訓練用了哪個資料快照沒有釘住——回頭想比較都比不了
  (這就是 08 文件 P0-A 剩餘的 immutable dataset version 的**最小消費場景**)。
- **單機最小版**:訓 head 時自動落「訓練清單快照」(sha 清單+標籤,`.lv_cache`);
  新 head 訓完先跑既有 probe_eval 對同一驗證集 → 新舊並排(per-class recall/escape)
  → 人按「採用」才替換(舊版留檔可回滾)。
- **客觀驗證**:合成——新資料實質提升 → 面板顯示提升且採用後生效;
  故意用壞資料訓 → 面板顯示退步,不採用則舊 head 原封不動。

### 2.3 標註品質收尾(組考卷/灰帶覆核整併完成)

- **為什麼**:功能地圖已宣告「標註者一致性/爭議裁決正整併於 📥」,但組考卷/灰帶覆核
  目前**隱藏中、整併未完成**——體檢卡 S1 是斷頭的(指向隱藏工具)、readback 的
  多人一致性沒人算。這不是新功能,是**收尾債**。
- **單機最小版**:📥 內給「一致性」小節:同批多標時算 agreement、分歧樣本進裁決
  小佇列;體檢卡 S1 從這裡取值(斷頭接回)。
- **客觀驗證**:合成雙標註(植入已知分歧率)→ agreement 數字 == 植入值。

### 2.4 標註預算規劃器(小,順手)

- **為什麼**:主管問「再標 500 張能換多少 recall?」——M5/probe_eval 的曲線其實
  已能外推,只差一個「輸入預算 → 讀曲線斜率 → 給預估區間+誠實免責」的面板。
- **驗證**:回顧模擬上外推 vs 實際,誤差區間如實顯示。

---

## 3. 想過但明確不建議(單機界線,防過度設計)

| 項目 | 不做的理由 |
|---|---|
| MES/資料庫即時接線 | CSV 手動丟已滿足;接線=平台級運維負擔 |
| 多人協作/權限 | 單機工具;真需要時走平台路線(08 文件 C 類) |
| 產線 inline 即時判定 | 本工具定位是離線分析/資料工作台,即時是另一個產品 |
| 自動重訓/自動換門檻 | 違反既定「只建議不自動」紀律 |
| SEM/光學跨模態對齊 | 需要配對資料+域專用 backbone,平台級 |
| 合成瑕疵生成(GAN/擴散) | 驗證成本極高、易自欺;先把真資料的迴圈榨乾 |

---

## 4. 建議優先序(impact × effort,單機尺度)

| 排序 | 功能 | 半導體價值 | AL 價值 | 工程量(粗估) | 前置 |
|---|---|---|---|---|---|
| 1 | 1.1 晶圓空間視圖+聚集檢定 | ⭐⭐⭐ | ⭐ | 中(視圖+一個統計量) | **需確認資料有座標欄** |
| 2 | 2.1 群組批次標註 | ⭐⭐ | ⭐⭐⭐ | 中(佇列 UI+帳本擴充) | 無 |
| 3 | 1.2 Tool/Recipe 顯著性 | ⭐⭐⭐ | ⭐ | 小(M19 檢定框架複用) | M20 已鋪好 |
| 4 | 2.2 模型晉升閘門+資料快照 | ⭐⭐ | ⭐⭐ | 中 | 無(順帶解 immutable version 最小版) |
| 5 | 1.3 時間軸/excursion | ⭐⭐ | ⭐ | 中 | M10 已鋪好 |
| 6 | 1.4 成本敏感工作點 | ⭐⭐ | ⭐ | 小 | 無 |
| 7 | 2.3 標註品質收尾 | ⭐ | ⭐⭐ | 小~中(整併債) | 無 |
| 8 | 1.5 瑕疵案例庫 | ⭐⭐ | ⭐ | 中 | 策展日誌已有一半 |
| 9 | 2.4 預算規劃器 | ⭐ | ⭐ | 小 | 無 |

**建議的下三輪**(若照此清單走):
- **下一輪**:1.2 Tool/Recipe 顯著性(最小工程量吃到最大製程端價值,M19+M20 全鋪好)
  ＋ 順手做 1.4 成本工作點(同屬「把既有分數變成決策」)。
- **再下一輪**:1.1 晶圓空間視圖(先跟你確認座標欄位存在)或 2.1 群組批次標註(不依賴資料格式)。
- **第三輪**:2.2 模型晉升閘門(把 AL 迴圈的「學得到」補上最後一塊信任)。

---

## 5. 一句話總結

> 這個專案的影像端(探索/審計/檢索/選樣/回讀)已經很完整;**還缺的集中在兩個方向**:
> 往**製程端**走一步(空間座標、tool/recipe 顯著性、時間軸、成本工作點——讓結論從
> 「影像有差異」變成「該去查哪台機、值不值得查」),以及把 AL 的**確認端**再降一個
> 量級(群組批次標註)並補上**信任端**最後一塊(模型晉升閘門)。共同前提都已在
> M20 鋪好(metadata 關聯)——這正是當初把它排第一的原因。
