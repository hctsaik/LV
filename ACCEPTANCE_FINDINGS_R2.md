# 驗收報告 R2：端到端深度走查（真實 DINOv2）

> 日期：2026-06-20　|　接續第一輪 [ACCEPTANCE_FINDINGS.md](ACCEPTANCE_FINDINGS.md)
> 差異：第一輪是宏觀 jobs-to-be-done 枚舉＋判定；**這一輪真的用真實 DINOv2（vits14, 384-d）把核心鏈路跑到底**，重點在「第一輪修的東西，使用者真的走得到終點嗎」。
> 真實資料：valid（55 圖 / 309 物件）、test（107 圖 / 550 物件）、10 類，valid 完全沒有 couch。

---

## 這輪實測（object-level、真實 embedding、859 物件）

我把「發現缺 couch → 用 test 補」這條第一輪縫好的路，**真的跑了一遍**：

| 路徑 | 結果 | couch 命中 |
|---|---|---|
| `reference_coverage`（參照分佈） | recall=**8.9%**、uncovered=**501/550** | **58/58** 全標 uncovered，且排最前 |
| `rank_gap_fillers`（採礦池） | 550 全排序 | couch 排 **first=0**；但前 58 名只 **26/58** 是 couch |

`reference_coverage` 的 uncovered 類分佈：
`cabinetDoor 144、chair 86、couch 58、window 54、cabinet 51、table 47、door 34、openedDoor 19、pole 8`

---

## Findings

### ✅ R2-1（好消息，先講）：整類缺失的「發現→撈候選」**技術上端到端可行**
第一輪我擔心「補洞基於自指稀疏度，撈不到一個點都沒有的 couch」。**實測證明這個擔心是錯的**：
- 參照分佈把 **couch 58/58 全部標為 uncovered**，且按該補程度排序時 couch 在最前。
- 採礦池 `rank_gap_fillers` 也把 couch 排第一（離 valid 最遠＝最該補）。

所以「撈得到 couch」。L2 normalize + 固定參考系的修復讓這條路在真實資料上成立。

### 🔴 R2-2：參照分佈的訊號被嚴重稀釋——真缺的類淹沒在「有但實例不同」裡
recall=8.9%、uncovered=**501/550**。問題在 **radius（valid 自身 1-NN cosine P75 = 0.338）對「跨資料集」太嚴**：test 的 cabinetDoor 即使 valid 有 145 個，實例不同也常 >0.338 → 被算成 uncovered。結果：
- uncovered 類分佈把 **cabinetDoor（valid 明明有 145 個）排第一（144）**，couch（真缺）只排第三。
- 使用者看「最該補的是 cabinetDoor」會**被誤導**——他其實不缺 cabinetDoor。
- 501 個散亂 uncovered 物件，使用者**無法直接行動**。

> 真正乾淨回答「我缺哪一類」的，反而是第一輪那張摘要卡的**按類差集**（couch，類級、零雜訊），而不是嵌入級的 uncovered。

### 🔴 R2-3：「發現」與「補」之間有一道斷層（類級 ↔ 物件級散點）
- **發現端**（摘要卡）說的是**類**：「缺 couch」。乾淨、可懂。
- **補洞端**（參照分佈 / 採礦池）給的是**物件級散點**：501 uncovered / 550 ranked，沒有「按類聚合」。
- 使用者從「缺 couch」走到「拿到 couch」之間，得在幾百個混雜候選（couch 夾雜 table/cabinet/chair）裡**自己用肉眼辨認**。

缺一個「按缺失類聚合的補洞視圖」：點 couch → 只給我 test 的 couch 候選 → 一鍵入購物車。目前這一段是斷的。

### 🔴 R2-4：`逐類別・區塊補洞` 模式對「整類缺失」完全無能
objcov 的類別表是用 **valid 現有物件類** 建的（present=9 類，**沒有 couch 行**）。所以：
- 在「逐類別・區塊補洞」模式裡，使用者**根本看不到、也補不了 couch**——因為它按「你已經有的類」分塊。
- 整類缺失**只有**「覆蓋散點・參照分佈」碰得到。
- 但兩個子模式長得很像，使用者很可能進「逐類別補洞」找 couch，然後一無所獲、卻不知為何。應在 objcov 類表頂端補一句：「classes.txt 還有 couch 等類在本資料集 0 個 → 去『覆蓋散點・參照分佈』補」。

### 🟧 R2-5：第一輪縫的「➜參照分佈」按鈕，還不夠端到端
它只做了「切模式 + toast」，缺三件讓它真的端到端的事：
1. **沒切 object-level**：整類缺失補洞要在物件級才精準（實測 couch 58/58 是 object-level；image-level 會更糊），但按鈕停在預設 image-level。
2. **沒自動載入候選**：還要使用者自己去右側選 test 資料夾。
3. **撈到之後沒接上購物車**：uncovered 物件沒有「一鍵全加」。

從「看到缺 couch」到「couch 進購物車」，目前仍是多步手動、且容易在 R2-4 那裡走錯模式。

### 🟧 R2-6：recall=8.9% 是強烈訊號，系統卻沒把它「翻譯」給使用者
valid 作為一個 split 只覆蓋 test 的 8.9%——這其實在說「**你的 val set 對 test 的代表性嚴重不足**」（valid 太小又偏、整類缺 couch、chair 只有 test 的 1/7）。系統**算得出這個數字，卻只顯示一個百分比**，沒解讀成使用者能行動的結論。一句「⚠ val 僅覆蓋 test 8.9%，代表性不足，跨版本比較的 recall 不可信」會比裸數字有用得多。

---

## 建議（依痛感，且都承接第一輪）

1. **【補上發現→補的最後一哩】** 做一個「按缺失/稀少類聚合的補洞視圖」：摘要卡點 couch → 直接列 test 的 couch 候選（用 label 過濾 uncovered/ranked）→ 一鍵全加購物車。把 R2-3 的斷層接起來。
2. **【參照分佈去雜訊】** uncovered 預設**按類聚合 + 標出「valid 此類數量」**，讓「真缺（valid=0）」和「有但實例不同」一眼分開；radius 給使用者一個「鬆/緊」拉桿並解釋含義（R2-2）。
3. **【防走錯模式】** objcov 類表頂端標明整類缺失的類要去參照分佈補（R2-4）；把「➜參照分佈」按鈕補成「切 object-level＋參照分佈＋提示載入候選」（R2-5）。
4. **【把數字翻成結論】** recall 低於門檻時，直接講「代表性不足」而非只給百分比（R2-6）。

---

## 一句話總結

第一輪把「**告訴你缺什麼**」做好了（摘要卡按類差集，乾淨）；這一輪實測發現「**幫你補**」這端雖然技術上撈得到 couch，但輸出是**物件級散亂、訊號被 radius 稀釋、還會在 objcov 模式整個消失**。
**真正缺的不是演算法，是把「類級的發現」和「物件級的補」縫成一條使用者點得下去的路**——這是第一輪縫合按鈕開了頭、但還沒走完的最後一哩。
