# KICKOFF — 貼給接手開發 AI 的第一則訊息

> **本檔不是第二份正典。** 唯一開發交接正典是 [06_開發交接指南.md](06_開發交接指南.md);
> 本檔只是「開工提示詞」:把當下(2026-07-12)的任務解鎖狀態與操作迴圈,濃縮成一段
> 可直接貼給接手 AI 的訊息。任務狀態改變時,以 06 與 ROADMAP 為準,先更新下方
> 「今日解鎖狀態」再交付;兩邊矛盾時 06 贏。

## 使用方式

把「──以下原封不動貼給接手 AI──」之後的全文,貼給接手的 AI 當第一則訊息即可。

──以下原封不動貼給接手 AI──

你是 repo `c:\code\claude\LV\visuallatent` 的接手開發 AI。假設你的能力有限:
**不要推理超出指派範圍的事,遇到停止條件就停下回報,不要自行變通。**

## 第一步:讀文件(只讀這兩份)

1. repo 根目錄 `CLAUDE.md` —— 開發協定,最高優先。
2. `5_active_learning_product_review_2026-07-12/06_開發交接指南.md` —— 唯一交接正典
   (含硬規則、已驗證現況快照 S-1~S-14/G-1、資料契約對表、任務卡 T0–T6、回報格式)。

其他文件**除非任務卡點名,不要讀、不要自行探索**。已驗證的事實都在 06 第 3 節,
附精確 file:line,不要重新推導。

## 今日解鎖狀態(2026-07-12)

| 任務卡 | 狀態 | 說明 |
|--------|------|------|
| T0 gate 治理 | 🚫 人工前置 | 你不可執行。gate 因契約檔雜湊變更判 RED → 停工回報 |
| T1 文案統一+死碼 | ⏸ 待放行 | 需 PO 先在 ROADMAP 決策日誌記一行;動工前先問人類是否已記 |
| T2 M9 GUI 接線 | 🔒 被擋 | 被 T0 與 `/pm` 紅測擋住 |
| T3 讀回落地層 | 🔒 被擋 | 需先立案(`/user` → `/po` → `/architect` → `/pm`),拿到設計與紅測前禁動 `apply_readback` 週邊 |
| T4 ANnoTation closeout | ⏸ 待指派 | 另一 repo,需人類明確指派才可動 |
| T5 task 顯式化+直送鈕 | 🔒 被擋 | 被 T2 擋住 |
| T6 review 真相調查 | ✅ 可做 | 唯讀調查,不改任何程式碼,產出 `07_review_truth_trace.md` |

## 操作迴圈(每張卡都一樣)

1. 領卡(一次只做一張,依 06 的順序)。
2. 逐條覆核該卡「前置」,任何一條不成立 → 停,回報。
3. 只動該卡白名單列出的檔案。
4. 跑該卡的驗收指令(gate / grep / 冒煙),把輸出原文留下。
5. 用 06 第 10 節的固定格式回報。
6. **停,等人類確認後才領下一張。**

## 停止條件速記(完整版在 06 第 2 節,違反=立刻停工回報)

- 不改 `tests/`、`3_Architect_Design/`、`4_PM_Feedback/`、`verify/`、`.claude/`、`.unet/`
  (連測試裡的字串都不行,需要同步就回報 `/pm`)。
- 不寫使用者資料集;一切進 `.lv_cache/` 或 `output/`。
- gate RED 且原因是契約檔竄改 → 禁止 `--snapshot`,停工回報。
- 不發明 schema:欄位一律以 06 第 4 節契約對表的右欄為準。
- 不做 06 第 9 節「明確非目標」的任何事。
- 本訊息與 06 或 `CLAUDE.md` 矛盾 → 停工回報,以 `CLAUDE.md` > 06 > 本訊息為序。

## 你的第一則回覆只准包含

(a) 你讀完了哪兩份文件;
(b) 你認定目前唯一可執行的任務卡與理由;
(c) 該卡前置條件的逐條覆核結果。
**不要開始寫任何程式碼。**
