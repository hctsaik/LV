# 驗收報告：Dataset Analysis Tools（完全使用者立場 · E2E 走查）

> 日期：2026-06-20　|　驗收者：以「資料集擁有者／ML 工程師」身分假設情境
> 真實資料：`indoor/[Small]/valid`（55 圖 / 309 物件）、`indoor/[Small]/test`（107 圖 / 550 物件）、10 類

## 驗收方法與態度

我以**使用者要達成的目標（jobs-to-be-done）**為綱，而不是「系統有哪些功能」為綱。對每個情境問三件事：
1. 我帶著這個目標進來，**走得到終點嗎**？
2. 路上**卡在哪、被什麼困惑**？
3. 就算系統做得到，**它有沒有主動把我該知道的事告訴我**？

證據分兩種，文中標注：
- **【實測】**＝用上面兩個真實資料集取證（類別解析、快取落點、類別分佈）。
- **【走查】**＝以系統行為＋使用者旅程推演（未逐一點 GUI，但對照真實資料判斷）。

---

## 真實資料畫像（後續情境都以此為例）

| 類別 | valid | test | 觀察 |
|---|--:|--:|---|
| cabinetDoor | 145 | 179 | 兩邊都最多 |
| door | 63 | 34 | |
| window | 53 | 63 | |
| chair | 12 | **87** | valid 嚴重偏少 |
| table | 11 | 47 | |
| cabinet | **5** | 52 | valid 幾乎沒有 |
| couch | **0** | 58 | **valid 完全沒有** |
| refrigeratorDoor | 6 | 2 | 兩邊都 < 樣本門檻 |
| openedDoor | 7 | 20 | |
| pole | 7 | 8 | |
| **合計** | **309** | **550** | valid 內 cabinetDoor:cabinet ≈ **29:1** |

這份資料天生帶著三個最常見的真實病徵：**整類缺失**（couch=0）、**極度不均衡**（29:1）、**樣本過少難分析**（refrigeratorDoor）。很適合用來壓測「完整度／比較」這條主線。

---

## 🔴 重大 finding（先講最痛的）

### F-CRIT-1　資料集被寫入 `object_crops/`——踩到系統第一鐵律

- **【實測】** `test/object_crops/` 真實存在，內含 `pad12_arv1`（**舊 arv1 格式**）、`pad12_r224_cls_arv2`、`pad12_r224_meanpool_arv2`、`_autotune`，檔案 mtime = 2026-06-19。`valid/` 乾淨。
- app 端目前已正確寫到 `.lv_cache/test_61ba23ebde/…`（修復到位）。但：
  - **歷史污染**：`test/object_crops/` 是修復前的舊版 app 寫進去的，**至今仍躺在使用者資料集裡**，會被 `git add .` 帶進 check-in——正是本專案最在意的事。
  - **仍存活的違規**：[`scripts/object_eval.py:402`](scripts/object_eval.py#L402) 的 CLI 仍寫 `root / "object_crops" / "_autotune"`（**資料集目錄**），沒跟上 app.py 改用 `_dataset_cache_dir`（`.lv_cache`）。只要有人跑 `python scripts/object_eval.py --dataset <data>`，就會再次弄髒資料集。
- **影響**：違反 `no-dataset-writes` 這條最高約束；使用者無從得知自己的資料夾被改動。
- **建議**：①清掉 `test/object_crops/`（既有污染）；②把 `object_eval.py` 的 `cache_dir` 改走 `.lv_cache`（與 app 一致）；③`compare_distributions.py:56` 已知要略過 `object_crops/`，反證系統「知道」這目錄是髒的——更該從源頭不寫。

### F-CRIT-2　「我的資料缺哪一類」這個最基本問題，預設視圖答不出來

- **【實測】** valid **完全沒有 couch**。但完整度的稀疏度是**自指的**（一個點跟自己鄰居比），對「整個資料集都沒有的類別」天生盲（沒有點，就沒有稀疏訊號）。
- 要發現 couch 缺口，使用者**必須**：載入第二資料夾 test → 把它的角色切成「參照分佈」→ 才會看到 recall 缺口。這條路**沒有任何預設引導**，新使用者不會知道。
- **影響**：使用者打開「完整度熱力圖」最直覺的期待就是「告訴我缺什麼」，而系統對「整類缺失」這種最嚴重的缺口**預設靜默**。
- **建議**：載入單一偵測資料集時，直接拿 `classes.txt` 的全集和實際出現的類別做差集，主畫面一句話：「⚠ classes.txt 有 10 類，本資料集只出現 9 類，缺：**couch**」。這是零成本、最高價值的提示。

---

## 情境驗收（逐 job-to-be-done）

### A. 我剛拿到資料集，想先搞懂它長什麼樣
- **判定：⚠ 摩擦**
- **【實測】** 類別名能正確解析成 `door/cabinetDoor/...`（即使 classes.txt 在第三層 `indoor/`，靠 `read_classes_txt` 多看一層僥倖命中）。
- **finding**：① 進來第一眼沒有「資料集摘要卡」（幾張圖／幾物件／幾類／每類幾個／不均衡度）。這些我得自己去不同工具拼。② 類別解析靠「多看一層」的巧合，**再深一層（或命名不同）就會退化成數字**——應該讓「資料集摘要卡」一併顯示「類別來源：indoor/classes.txt」，解析失敗時明確提示去手動指定。

### B. 嵌入空間長什麼樣？有哪些簇／離群？
- **判定：✅ 大致滿足**
- Visualize 的框選看圖、離群排序、3D/投影方法齊全，這是系統最成熟的一塊。
- **finding**：冷啟動要先算 DINOv2 embedding；55 張可接受，但**真實上千張的資料集首次 Run 的等待沒有預估時間**，使用者會以為當掉。建議首跑顯示「約 N 張、預計 ~X 秒」。

### C. 有沒有重複／train-val 洩漏？
- **判定：✅ 滿足（但藏太深）**
- 右欄「重複」tab 有 phash 嚴格 + embedding 語意 + 「僅跨 split」洩漏掃描，設計到位。
- **finding**：洩漏掃描是資料健康的**頭等大事**，卻藏在右欄 tab 裡，且要使用者自己想到去開。應該在「資料集摘要卡」就跑一次輕量洩漏體檢並紅字示警。

### D. 哪些標籤可疑（標籤分歧）？
- **判定：✅ 滿足**
- 標籤分歧排序 + 一鍵送灰帶覆核，串接清楚。

### E. 我的資料夠全嗎？哪裡缺／假完整？
- **判定：⚠ 半滿足（被 F-CRIT-2 拖累）**
- **【實測】** 屬性棋盤能抓「不均衡」（cabinetDoor 145 vs cabinet 5）和「假完整／近重複」，這是它的獨門。逐類別・區塊補洞能抓「類內變體缺口」。
- **finding**：① 「整類缺失」這種最嚴重的缺口反而抓不到（見 F-CRIT-2）。② refrigeratorDoor（6）、cabinet（5）會落入「< 4 難分析」或樣本過少，使用者只看到一個 ⚠ 而沒有「該怎麼辦」。應給出可操作下一步（去 test 撈／標更多）。

### F. 我想補資料——哪些能補我的洞？補了有沒有用？
- **判定：✅ 滿足，且是系統最強的差異化**
- **【實測】** 用 test 當採礦池補 valid 的洞，這正是兩資料集的真實用途；H1–H5「補資料有沒有用」的誠實閘是少見的好設計（避免盲目收集）。L2 normalize + 固定參考系的修復也讓同類候選不再被投到不相干區塊。
- **finding**：補洞結果是「提議→購物車／Labeling」，符合不寫資料集的約束 👍。但**補洞前的引導不足**：使用者要先知道「我缺 couch」(F-CRIT-2) 才會想到來補。缺口偵測與補洞應該在同一個流程裡銜接：「偵測到缺 couch → 一鍵去 test 撈 couch」。

### G. valid vs test 分佈像不像（drift）？
- **判定：✅ 滿足**
- **【實測】** Compare 逐類別比物件分佈漂移，正好能抓出 chair（valid 4% → test 16%）、couch（0 → 58）這種大漂移。
- **finding**：① Compare（兩堆比）和完整度（一堆內看）最容易被使用者搞混——功能地圖已點出這對混淆，但**主畫面**仍要點開 popover 才看得到。② couch 在 valid 是「0 vs 58」，Compare 表裡會是個極端值，但**它跟完整度的 couch 缺口其實是同一件事**，系統沒把兩處連起來。

### H. 標註預算有限，先標哪些最值得？
- **判定：✅ 滿足**
- 右欄選樣 tab 的 farthest-point 主動學習到位。藏得深（同 C）。

### I. 以文／以圖搜圖
- **判定：✅ 滿足（有前提）**
- 以文搜圖需切換到 chinese-clip 模型；**finding**：使用者不會知道「要先換模型」，文字查詢框在非 CLIP 模型下應明確提示而不是靜默無效。

### J. 標註者一致嗎（組考卷）？
- **判定：⚠ 場景依賴**
- 設計正確（量具校準、餵 H1–H5 的 S1）。**finding**：單人使用者進來會困惑「我自己標的還要考自己？」——需要一句話講清楚「這是在量你判斷的穩定度，不是考你」。（已決定收進「進階」，方向正確。）

### K. 爭議樣本怎麼裁決＋留紀錄（灰帶覆核）？
- **判定：✅ 滿足，且是唯一的「治理」出口**
- 提議→雙簽→匯出、不寫回資料集，是六工具中唯一產生可追溯裁決的地方。**finding**：價值很高但入口和組考卷一樣會被單人使用者忽略；「改標只在這」這個賣點目前只在 popover 裡。

### L. 模型在哪些型態 recall 差（評估）？
- **判定：⚠ 有前提門檻**
- **finding**：要使用者自備「模型預測 CSV」+（可選）「共識子集 CSV」。對「只想看資料」的使用者門檻偏高，且和前面的探索工具是**兩個世界**——進來才發現要餵預測檔。應在入口就標明所需輸入。

### M. 跨工具收集→分流→匯出（購物車工作流）
- **判定：✅ 滿足**
- 策展購物車跨視圖累積、一鍵分流到組考卷／灰帶／CSV、策展日誌可重選——這條「探索→行動」的縫合是亮點。

### N. 資料衛生（系統不該弄髒我的資料）
- **判定：❌ 不滿足（見 F-CRIT-1）**
- app 端守住了，但 CLI 與歷史污染破功。這是對「資料集擁有者」最敏感的一條。

---

## 滿足度總表

| 情境 | 判定 | 主要缺口 |
|---|:--:|---|
| A 初識資料集 | ⚠ | 沒有摘要卡；類別解析脆弱 |
| B 嵌入探索 | ✅ | 冷啟動無預估時間 |
| C 重複／洩漏 | ✅ | 藏太深，未主動體檢 |
| D 標籤分歧 | ✅ | — |
| E 完整度 | ⚠ | 抓不到整類缺失；過少類別無下一步 |
| F 補洞＋H1–H5 | ✅ | 缺口偵測與補洞未銜接 |
| G Compare drift | ✅ | 與完整度易混淆、未互聯 |
| H 選樣 | ✅ | 藏太深 |
| I 搜圖 | ✅ | 換模型無提示 |
| J 組考卷 | ⚠ | 單人情境定位不清（收進階） |
| K 灰帶覆核 | ✅ | 入口易被忽略 |
| L 評估 | ⚠ | 需自備預測檔，門檻高 |
| M 購物車工作流 | ✅ | — |
| **N 資料衛生** | ❌ | **CLI 仍寫資料集 + 歷史污染** |

---

## 給產品的三個優先建議（依痛感排序）

1. **【必修】堵住資料集污染**：清掉 `test/object_crops/`，把 `object_eval.py` 改走 `.lv_cache`。這是信任問題，優先於一切功能。
2. **【高 CP】補一張「資料集摘要卡」**（進來就看到）：張數／物件數／每類數量＋不均衡度、**整類缺失差集（couch！）**、輕量洩漏體檢、類別來源路徑。一張卡同時解掉 A、C、E(整類缺失)、I 的入口問題，並把目前藏在各工具裡的關鍵訊號浮上檯面。
3. **【體驗】把「偵測到缺口 → 去補」縫成一條路**：完整度發現缺 couch → 直接「去 test 撈 couch」→ 購物車。目前這三步分散在三個地方，使用者要自己想到。

---

## 一句話總結

**這個系統「能做的事」很全、且有幾個少見的好設計（H1–H5 誠實閘、不寫回資料集的治理出口、量具校準）；但它預設「不主動告訴你最該知道的事」**——最嚴重的缺口（整類缺失、洩漏、被弄髒的資料夾）都要使用者自己想到去翻。把這些訊號從「藏在工具裡」提升到「進門就看到」，體感會從「一套專家工具」變成「一個會主動體檢的助手」。
