# 05|AL 迴圈 multi-agent 討論記錄(保留分歧原貌)

> **本文件為 multi-agent 討論產出,尚未開發。** 非開發指令;所有 P0 項目(含 M9–M13)開工仍待 User 放行,並依 CLAUDE.md 由 PO 記入 ROADMAP 決策日誌後走五階段流程。

## 本文件回答什麼

1. 針對「讓 CV 工程師順跑 active learning、釐清 User×CV 工程師協作權責」,三個視角提案(A:CV 工程師動線、B:標註協作權責、C:半導體場景)各自主張什麼。
2. 兩位反方(守成派架構師、務實產線顧問)推翻或修正了哪些主張,理由與代價證據為何。
3. 主席最終裁決的取捨(全文照收,含 P0/P1/P2 與不做/緩做清單)。
4. 哪些分歧**本次沒有解決**,留待日後驗證翻案——這是本記錄存在的主因:若後續證據推翻某前提,可回此處找到當初完整的反對理由。

優先序標記:P0/P1/P2;定位標記:【既有元件重排】【M9–M13 已規劃】【新需求須立案】。file:line 為各方查證時所引,本文撰稿時已抽查關鍵引用屬實(見文末未解清單第 2 條之例外)。

---

## 一、三提案摘要

### 提案A:CV 工程師視角(每日跑 AL 的動線)

- 現況一輪 AL 要跨 6 頁 + 2 個外部動作,狀態全靠人腦記。
- **最痛斷鏈**:`apply_readback` 只改記憶體 `viz_records`(labeling_handoff.py:404-406),匯出讀的是盤上 label 檔——修正標籤進不了訓練集,重 Run 即蒸發,「這輪等於白標」。→ P0【新需求須立案,08-13 均未涵蓋】
- 框級標註丟失:讀回只取 `shapes[0].label`(labeling_handoff.py:287-288),detection 的框全丟。→ A 視為高優先;裁決降為 P1 分段【新需求須立案】
- 評估閉環不存在:評估工具被藏(app.py:8094-8099)、`learning_curve`/停止準則在 active_loop.py 但 app.py 零引用——「何時停」全憑感覺。→ P0【M12 已規劃】
- 無 pool_registry/round_ledger(哪些 sha 送過、第幾輪皆無記錄);「不用回 LV」文案與 📥 分頁互相矛盾(7205 vs 7256);sha 不匹配靜默(7292);批次永不結案(`delete_pending` 零呼叫)。→ P0【M9/M10 已規劃 + 既有文案重排】
- 兩套選樣不互通(Visualize 純多樣性 vs 瑕疵③三訊號),提議**合併為單一挑樣元件**掛 M11。→ 裁決否決 UI 合併,改後端共用純函式,P1【M11 已規劃】
- 更名清單:「匯出清單」→「🛒購物車」、工具「匯出」→「組訓練子集」、行話更名。→ 裁決緩做 P2【既有元件重排】
- 低風險修:清功能地圖殘留(8119-8123)、刪 `_render_cmp_panel` 死碼(4595-4648)、瑕疵③補直送 Labeling 鈕(1968-1970 只進購物車,復用 7190)。→ P0 文案/P1 直送鈕【既有元件重排】
- 理想儀表板:📥 分頁擴建「🔁 迴圈」單頁,五欄狀態機(挑樣→送標→回讀→評估→下一輪)+ 常駐 Round 狀態列;點名純新缺口:讀回落地層、回合常駐顯示、預標寫入 handoff、評估遺產(漏抓畫廊+共識靶)併入回合報表。

### 提案B:標註協作視角(權責與可見性)

- 盤點六大權責不清決策點:8 個送標入口全是 CV 單方按鈕、無審批無防重複(app.py:2406、2705、2783、3283、6329;7193-7198);task 語義由排序選單**隱式推導**(2403-2404);唯一裁決管線 TASK_ADJUDICATE 掛死在隱藏工具(7641/7790);few-shot 正常確認無 User 覆核出口(1728-1740);bank 維護全在工程師 session(1618-1726);停止準則無 GUI(active_loop.py:107)。
- 可見性缺口:CV 工程師**半盲**(status 三態有定義不顯示 labeling_handoff.py:53-55;不知哪幾張未標;014_process.py:63-67 把全部 open 批次一次標 `read_back`,兩邊各自解釋);標註者**全盲**(`_handoff.json` 帶 task/instructions/payload 但 module_012 完全不呈現,adjudicate 與 fresh 長得一樣)。
- 提出兩角色 RACI 表 + **最小顯性化資訊集**:round_id 寫入 `_handoff.json`/`_pending.json`、批次來源+一句選樣理由、四態 `sent→annotating→exported→applied`(拆掉語義重載的 `read_back`)、annotator(labeling_handoff.py:294 LV 已讀但無人寫)。→ P1【既有契約欄位顯性化=既有元件重排;跨 repo 部分為小案】
- 核心主張:「權責明確化不需要新工具,而是把已存在但不可見的契約欄位在兩端 UI 顯性化」;並把「正常樣本確認」「爭議裁決」兩個領域判斷點**正式移交 User**。→ 顯性化獲全場背書;整包移交遭反方二駁回,裁決改採三分角色。
- task 顯式 selectbox(預設=現行推導值)。→ P1【既有元件重排】

### 提案C:半導體場景視角(產線情境映射)

- 列 9 個產線情境對映現有頁面:①AOI 後 ADC 分類複判(瑕疵②③)、②極端不平衡 rare-event(Normal Bank 對路,anomaly_tool.py:106-116)、③nuisance/過殺過濾(confirmed 只活在 session)、④NPI 冷啟動(novelty 失效,監督UMAP 預設 397 在零標籤池無意義)、⑤recipe 漂移(Compare 4862-4871 無門檻無趨勢)、⑥多機台成像差異、⑦稀有模式發現命名(離群 2438+以圖搜 2563)、⑧golden 樣本庫(bank 無版本血統)、⑨auto-binning 降複判負荷(should_stop_labeling 沒接 GUI)。
- 噪音判定:train/val 洩漏檢查、以文搜圖(chinese-clip,2569)、組考卷結構、監督UMAP 預設——對產線是噪音;「桶①佔比」應重新包裝為「光學可偵測性」、Compare 應包裝為「lot 漂移監控站」、隱藏「評估」正是 ADC 驗收報表應以 M10/M12 回歸。
- **三缺口排序**:1)框級回程斷裂(labeling_handoff.py:286-289 幾何全丟 + apply_readback 不落地)——「沒有這條,整個偵測 AL 不成迴圈」→ 裁決採反方二改為分段,P1;2)無帳本(08/09 未開工,active_loop.py:64,107 無資料底座,「自動化率到了沒」是採購級問題)→ P0【M9/M10 已規劃】;3)分數治理缺席(probe_eval 未接 GUI、TASK_ADJUDICATE 藏死 7790,半導體要可簽核的 overkill/underkill 率)→ P0【M12 已規劃 + 部分新需求】。

---

## 二、兩反方批判摘要

### 反方一:守成派架構師(逐條檢驗合併/更名/重組)

- 核心立場:**斷鏈類是真問題,更名合併類多半代價被低估**;全數主張經 Read/Grep 實查。
- 修正事實:「不用回 LV」實為 8 處非 11(app.py:2412,2712,2787,3289,6336,7205,7234);功能地圖假地圖屬實(8108-8123 教用戶開被 8098 擋掉的工具);`_render_cmp_panel`(4596)確為零呼叫死碼。
- **反對 A3 更名**:再造第三名是加劇不是收斂;「匯出清單/送整車」在 tests/e2e 出現 8 處/涉 10 檔,文字選擇器全斷;8096-8097 的 `匯出子集→匯出` session 正規化就是上次更名的永久疤痕——每次更名都養一條遷移碼。
- **反對 A4 UI 合併**:M7 才修過 confirmed 跨①②索引空間污染;兩頁 record 空間不同(整圖 vs 物件 crop);st.tabs rerun 重置是已知系統性 flaky。正解=M11 做純函式,兩頁各自呼叫,UI 不動。
- **A8 有條件**:TASK_ADJUDICATE 呼叫點(7642,7791)仍是活契約;刪除/降級是 ROADMAP 層決策,PO 先記決策日誌,PG 不得先動。
- 支持:A1/A2/A5(文案+死碼)、A7 直送鈕(條件:送原圖路徑非 crop——object-export-crop-bug 教訓;key 進 anomaly 命名空間)、B 顯性化(annotator 294 已讀、STATUS_SENT 273 已存在,純加法)、C 三缺口為最高優先。
- `read_back` 拆態:用**新增狀態值**而非改名,舊 `_pending.json` 向後相容;跨 repo + nativeApp vendored 三處同步,成本別低估。
- 總結排序:先斷鏈修復 → 加法(A7/B) → 緩更名(A3/A6) → 不做 UI 合併(A4)、不加第七工具列項。

### 反方二:務實產線導入顧問(真痛點 vs 簡報想像)

- 真痛點:#1/#3/#9 是同一條 ADC 主線,採購理由=overkill/underkill 率;但 confirmed 與讀回全活在 session(app.py:7275-7281、labeling_handoff.py:400-413)——**「換班即失憶」在三班制產線是致命傷,C 只當第 3 缺口太輕**;#8 golden 庫被低估(變更管理要求可簽核,不只血統)。
- 簡報想像:#5 lot 漂移歸 SPC/FDC(MES/CIM 既有系統),離線 Streamlit 不會被排進生產流程;#7 新 mode 命名是 MRB 跨部門會議決定;#4 NPI 期本來就全檢;#6 是設備工程量測題。
- **三提案共同盲區**:全部假設影像已躺在資料夾——真實產線影像掛在 klarf/機台 FTP,lot/wafer/die 座標是第一公民,LV 無任何 metadata schema,這比任何 UI 重構都前置。另:**審計缺席**——改標留痕僅一次性 dataframe(app.py:7283-7292)不落盤,annotator 無人寫入。
- RACI 批判:「User」至少拆三人——**複判員**(秒級判 real/nuisance,不會也不該操作 6 頁工具)、**製程工程師**(defect 判準;「什麼算正常」是製程 spec,複判員無權定義)、**品保**(門檻簽核、爭議走 MRB)。「何時停」的 A 是標註預算問題,在主管/品保不在 CV。B 整包移交落地時會全塞給標註外包,權責照舊不清。
- 最小可行 3 件事:1)讀回落地 `.lv_cache` 覆蓋層 + append-only 變更日誌;2)08/09 開工 + 拆 014 全批 `read_back`(014_process.py:63-67);3)可簽核 overkill/underkill 報表(probe_eval 接 GUI + per-class recall + CSV)——「沒這張報表,廠內立項過不了」。
- **反直覺結論**:C 排第一的框級回程可以緩——ADC 大宗是預裁切 crop 的分類複判,分類字串讀回足以跑通第一階段;先讓分類迴圈可審計地轉起來,框級留給 detection 段。

---

## 三、主席裁決全文(照收)

# AL 迴圈裁決書(主席收斂版)

查證基準:所有 file:line 均經本席 Read/Grep 複核(如 `apply_readback` 僅改記憶體 labeling_handoff.py:400-413、`shapes[0]` labeling_handoff.py:287-288、隱藏工具正規化 app.py:8094-8099、`TASK_ADJUDICATE` 僅存活於隱藏頁 app.py:7642/7791、`_render_cmp_panel` 4596 零呼叫、task 隱式推導 2403-2404、瑕疵③僅進購物車 1968-1970、014_process.py:63-67 全批標 `read_back`、app.py 僅 import `confusion_targeted_priority`(1541)、「不用回 LV」實為 8 處文案)。

## 1. 每頁/每功能模糊點的處置清單

| 對象 | 問題 | 處置 | 優先 | 反方異議與化解 |
|---|---|---|---|---|
| 讀回套用(labeling_handoff.py:400-413) | 只改記憶體,重 Run 白標蒸發 | **落地 `.lv_cache` 標籤覆蓋層+append-only 變更日誌(舊標/新標/annotator/時間戳),匯出可選「含修正」** | **P0** | 四方一致,無異議;同時解 A 最高缺口與反方二審計否決項 |
| 「不用回 LV」文案(2412/2712/2787/3289/6336/7205/7234)vs 📥(7257) | 正典矛盾 | 統一為「送出→ANT 標→回 LV 套用」 | P0 | 反方一支持;化解=同 PR 掃 tests/e2e 可見字串 |
| active_loop/probe 未接 GUI(app.py:1541 僅一函式) | 「還要標多少/自動率」答不出 | M12 probe_eval 接 GUI+per-class recall+overkill/underkill CSV | P0 | 反方二列為採購級門檻,無異議 |
| pool_registry/round_ledger(08/09,規格已備) | 重複送標無防呆、回合不可追、換班失憶 | 依規格開工(**仍待 User 放行**);順手拆 014 全批 `read_back` 語義 | P0 | 無異議;開工程序走 CLAUDE.md 閘門 |
| 框級 shapes 讀回(287-288 只取 `shapes[0]`) | 幾何全丟 | **分兩段**:分類字串迴圈先落地(P0 上列);shapes 幾何讀回配 M13 預標框再做 | P1 | 採反方二「先讓分類迴圈可審計地轉起來」,化解 C 的第 1 排序 |
| 功能地圖指隱藏工具(8101 區塊)+ `_render_cmp_panel`(4596) | 假地圖、死碼 | 清文案、刪死碼;**不刪** handoff 函式(7642/7791 活契約) | P0 | 反方一 A2/A5 支持、A8 保留條件已納入 |
| task 語義(2403-2404 隱式推導) | 標註者不知任務 | 顯式 selectbox,預設=現行推導值 | P1 | 反方一支持,行為不變 |
| 四態/round_id/annotator/選樣理由 | CV 半盲、User 全盲 | 兩端顯性化(見 §3);`exported/applied` 用**新增狀態值**不改名 | P1 | 反方一條件:向後相容+三處同步(含 nativeApp vendored) |
| 瑕疵③只進購物車(1968-1970) | 最該送標的頁無出口 | 補直送鈕,復用 `_send_to_labeling_ui`(7190) | P1 | 反方一條件:送原圖路徑非 crop、key 進 anomaly 命名空間 |
| Visualize 選樣 vs 瑕疵③ 2×2 | 兩套訊號不互通 | **後端共用 M11 純函式,兩頁 UI 各自保留** | P1 | 採反方一 A4:M7 索引空間污染+st.tabs flaky 教訓 |
| 三個「匯出」撞名 | 認知負荷 | 不造新名,文案收斂到既有「匯出清單」 | P2 | 採反方一:E2E 選擇器 8 處/10 檔、8096 更名疤痕 |
| 行話(桶①佔比等) | 不可解 | 只改 caption/help;widget label 緩(E2E 命中) | P2 | 反方一條件化解 |
| 隱藏三工具 | 殘骸 | 評估→M10/M12 回歸;灰帶裁決掛回📥;組考卷降級 kappa | P2 | 反方一:屬 ROADMAP 決策,**PO 先記決策日誌**再動 |

## 2. AL 迴圈目標資訊架構

**落點:📥 分頁擴建為「🔁 標註迴圈」,不新增第七工具列項**(反方一;8098 正規化耦合)。CV 工程師動線:

- **常駐狀態列**:Round N|已送/已回/已套用/pool 未標。依賴 08+09;「作用中回合常駐顯示」為 09 自承缺口→**補規格**。
- **①挑樣**:既有 UI 原地保留(Visualize FPS、瑕疵③2×2),後端統一呼叫 M11 hybrid_sampler+M10 四佇列(FN/FP/混淆/低信心)。既有元件重排+M10/M11。
- **②送標**:復用 `_send_to_labeling_ui`(7190)+08 防重複攔截+09 回合戳記(規格已寫);**新需求(小案立案)**:dino_head/瑕疵分數與 M13 框以 sidecar 預標寫入 handoff。
- **③回讀**:復用 7264 批次清單,加四態徽章、sha 不匹配顯式報錯、結案鈕(接上零呼叫的 `delete_pending`)——加法;**全新需求(U-Net 立案,08-13 均未涵蓋)**:讀回落地層+變更日誌。
- **④評估**:M12 接 GUI+plotly 學習曲線;漏抓畫廊(評估工具遺產)併入 M10 回合報表。
- **⑤下一輪**:漏抓類→一鍵弱類配額餵回 `confusion_targeted_priority`(1541 已接);Compare 漂移表(4862)僅作回合參考指標。

ANT 端配套:module_012 頂部呈現 instructions/round/task(零新依賴);module_014 拆全批 `read_back`(014_process.py:63-67)。

## 3. User × CV 工程師分工最終版

**裁決「User 是誰」:採反方二三分**——複判員(秒級判定,只進 ANT,不碰 LV 六頁)、製程/領域工程師(defect 判準、正常樣本確認)、品保/主管(門檻簽核、預算與停止)。B 的整包移交駁回。

| 決策 | 複判員 | 製程工程師 | 品保 | CV | 系統 |
|---|---|---|---|---|---|
| 挑樣/送批 | I | C | I | **A/R** | 08 攔截 |
| task 宣告 | I | I | — | **A** | 寫入 handoff |
| 標註執行 | **R** | C | — | I | 預標 seed |
| 正常樣本/判準 | C | **A**(經 verify 批覆核,流程約定不寫死) | I | R | I |
| 爭議裁決 | R | **A** | C(重大走 MRB) | R(送裁決) | ledger 記錄 |
| 門檻/何時停 | — | C | **A** | R(供 probe 曲線) | 建議、禁自動停 |
| bank/重訓/reset | I | I | 簽核 golden | **A/R** | append-only |

**最小顯性化資訊集**(兩端同步,B 提案+反方二背書):round_id(寫入 `_handoff.json`/`_pending.json`)、source·task·一句選樣理由(module_012 呈現)、四態 `sent→annotating→exported→applied` 附未標清單、annotator(ANT claim_task 寫入,LV 294 已讀)。

## 4. 半導體場景 Top 5(採反方二修正後)

1. **ADC 複判主線(C#1/#3/#9 合併)**:排序複判+nuisance 回饋+auto-binning;缺口=讀回落地、confirmed 持久化、M12 報表。
2. **極端不平衡 rare-event(C#2)**:Normal Bank 對路;缺口=08 去重、2×2 鎖格時的 novelty 長跑支撐。
3. **Golden 樣本庫(C#8,反方二升格)**:缺口=bank 版本/血統/簽核——變更管理需求,需立案。
4. **稀有模式發現(C#7 縮小)**:離群→相似擴散→整簇送標保留;命名/schema 同步歸 MRB 會後人工回填,不做自動同步。
5. **漂移監控(C#5 降級)**:只做「可匯出報表」,排程/OCAP 歸 SPC/FDC。
**前置盲區**(反方二,採納):lot/wafer/die metadata schema 為全新需求,先於任何 UI 重構立案評估。

## 5. 不做/緩做清單

- **不做** A4 挑樣 UI 元件合併——M7 索引空間污染+st.tabs rerun flaky 實證(反方一)。
- **不做** 第七工具列項——肌肉記憶+8098 正規化耦合;落 📥 擴建(反方一)。
- **不做** lot 漂移自動排程——歸 MES/CIM,離線工具做不進生產流程(反方二)。
- **不做** 標註 UI 內命名→ANT schema 自動同步——mode 命名是 MRB 決策(反方二)。
- **不做** 裁量權整包移交籠統「User」——落地必淪為外包背鍋(反方二)。
- **緩做** A3/A6 更名——E2E 文字選擇器 8 處/10 檔+歷史更名疤痕(反方一);只統一稱謂。
- **緩做** 框級 shapes 讀回——分類迴圈先行,配 M13 預標框再做(反方二對 C#1 的反直覺結論,本席採納為「分段」而非「不做」)。
- **緩做** 隱藏三工具結構性移除——待 PO 決策日誌+M10/M12 落地(反方一 A8)。

**追溯註記**:本裁決僅為建議;08-13 開工仍須 User 放行、PO 依 CLAUDE.md 記入 ROADMAP 決策日誌後走五階段流程。

---

## 四、本次未解、留待驗證清單

1. **所有 P0 的前提未成立**:M9–M13 開工仍待 User 放行(見記憶 [[al-loop-specs-pending]]);本裁決僅為建議,PO 尚未記任何決策日誌。
2. **「不用回 LV」文案處數三說並存**:提案A 稱 11 處、反方一稱 8 處(列 7 個行號)、本文撰稿時 grep 實得 7 行(app.py:2412、2712、2787、3289、6336、7205、7234)。改文案 PR 前須以 grep 定案,並同步掃 tests/e2e 可見字串。
3. **反方二「框級回程可緩」的前提未驗證**:「ADC 大宗是預裁切 crop 的分類複判」是產線經驗推定;若實際目標資料集是 detection(如 fruit YOLO testbed),分類迴圈先行的假設可能不成立,C 的原排序應翻案。
4. **讀回落地層的具體設計未定**:`.lv_cache` 標籤覆蓋層 vs 直接寫 label 檔的邊界、與 [[no-dataset-writes]] 契約(絕不寫使用者資料集)的相容性、匯出「含修正」的合成規則——08-13 規格均未涵蓋,須新立案走五階段。
5. **09 規格自承缺口未補**:「作用中回合的常駐顯示」裁決要求補規格,尚未寫。
6. **lot/wafer/die metadata schema 範圍未評估**:反方二判為「比任何 UI 重構都前置」,但本次未討論 schema 長相、是否僅限半導體場景、與現有 records 結構如何相容。
7. **「User 三分」在非半導體場景是否過度設計**:複判員/製程/品保三分法源自產線組織;一般 CV 團隊(或 fruit 這類 testbed)可能仍是兩角色,RACI 表的可移植性未驗證。
8. **`exported/applied` 新增狀態值的三處同步未規劃**:LV、ANnoTation(014_process.py)、nativeApp vendored 雙副本(見記憶 [[lv-vendored-submodule-sync]])的相容性測試與遷移路徑,反方一僅警告成本、無人給出方案。
9. **M11 純函式後端共用是否足以讓兩頁訊號互通**:裁決以「不合併 UI」化解 A4 風險,但「兩頁各自呼叫同一 sampler」能否真正消除「兩套 AL 選樣各挑各的」問題,待 M11 實作後以真實資料驗證。
10. **漂移「行動門檻」無人給出數字**:Compare 漂移表(app.py:4862-4871)僅裁決為「回合參考指標」,門檻參考值留待 M12 probe_eval 落地後以真實資料校準。
11. **隱藏三工具的最終命運未定案**:評估→M10/M12 回歸、灰帶→📥、組考卷→降級 kappa 只是 P2 方向;TASK_ADJUDICATE(app.py:7642/7791)仍是活契約,結構性移除需 PO 決策。
