# 功能重疊與使用清晰度盤點(2026-07-19)

> 範圍:9 個顯示工具(Visualize / Compare / 完整度熱力圖 / 瑕疵偵測 / 🎯 以樣搜樣 /
> 🧪 差異探索 / 🩺 資料體檢 / 匯出 / 📥 標註回饋)+ 3 個隱藏(組考卷 / 灰帶覆核 / 評估)。
> 對照基準:2026-07-06 多 agent 盤點結論(「工具面幾乎都是刻意分開;重複在底層 plumbing」)。
> 本次聚焦 **M19 / M20 兩個新工具加入後的新格局**。證據 = 逐一核對 app.py 各工具的
> caption / 功能地圖文案 / 實際呼叫的底層函式。

## 0. 結論摘要

- **真功能重複:1 處**(M19 的 PCA 旁證 ≈ Compare 整張影像模式的子集)——可接受但要指路。
- **同一訊號多個面孔(不是重複,但使用者會問「差在哪」):3 組**——離群 ×3、
  重複/洩漏 ×2、coverage ×2。底層 plumbing 全部共用(零重複實作,好事),
  缺的是**畫面上的互相指路**。
- **使用不明確:2 個新增點 + 1 個殘留點**——「兩堆比較該用哪個工具」(Compare vs
  差異探索)、「匯出」一詞重載、體檢卡指向已隱藏的組考卷。
- 2026-07-06 的既定分工(以樣搜樣/找同款/以圖搜圖三分、選樣目標 caption 揭露)
  **維持有效,不需重開**。

## 1. 逐項盤點

### A.「兩堆比較」現在有兩個工具 —— 最需要指路的一組(新增)

| | Compare Distributions | 🧪 差異探索 |
|---|---|---|
| 回答 | 兩堆**整體**像不像(漂移分數+散點) | 兩堆在**哪個位置**系統性不一樣(patch 級) |
| 前提 | 不需對齊、不需標註(整張模式) | **拍攝對齊**(同站同視角) |
| 輸出 | 形心餘弦距離+A/B 散點+框選看圖 | 差異熱圖+Top-K 區域+統計判定(p 值) |
| 誠實機制 | 無統計檢定(描述性) | permutation null(「未發現穩定差異」會明講) |

**判定:刻意分開、正確**(一個是探索、一個是檢定)。
**問題**:使用者拿著「兩個資料夾」站在工具列前,沒有任何文案告訴他選哪個。
功能地圖的「最常搞混的兩對」段落寫於差異探索誕生前,未涵蓋這一對。

**建議(小文案)**:功能地圖「最常搞混」段補第三對:『**Compare**=兩堆整體像不像(不需對齊);
**差異探索**=對齊前提下差在哪個位置(有統計判定)』;兩工具 ❓ popover 各補一句互指。

### B. M19 PCA 旁證 ≈ Compare 整張影像模式(唯一「真重複」)

差異探索的「📎 整體旁證(PCA)」= 兩群 meanpool embedding → 非監督散點;
Compare 整張影像模式 = 同樣的兩堆整圖 embedding → 散點 + 漂移分數 + 框選看縮圖。
前者是後者的**弱化子集**(無框選、無漂移分數)。

**判定:保留**——旁證免費(直接用已算好的 patch 特徵取平均,零額外模型成本)、
且它的存在理由是「誠實內建」(讓使用者當場看兩群整體分不分得開),移除反而傷誠實性。
**建議**:旁證 caption 加一句「要框選看圖/漂移分數 → Compare Distributions(整張影像模式)」;
**不要**把旁證做大(那是重複建設)。

### C. 離群 ×3 個面孔(同訊號家族 ×2 + 不同語義 ×1)

| 面孔 | 訊號 | 用途 |
|---|---|---|
| Visualize「離群度」排序 | 整圖 embedding kNN 幾何距離 | 互動:排序/框選/送標 |
| 🩺 資料體檢「離群影像」 | 同上(compute_outlier_scores,同函式家族) | 報告:top-10 + method line |
| 瑕疵偵測 novelty | 到 **Normal Bank** 的距離 | 「偏離正常定義」——語義不同 |

**判定**:前兩者=同訊號雙面孔(報告 vs 互動,by design);第三者語義不同,不動。
**建議(小文案)**:資料體檢離群節加「要互動框選/送標 → Visualize 離群度排序」;
Visualize 離群排序 help 加「要一頁量化報告 → 🩺 資料體檢」。

### D. 重複/洩漏 ×2 個面孔

Visualize「重複」tab 與 🩺 資料體檢用**同一組函式**(`find_duplicate_pairs_phash/embedding`,
含 cross_split_only 洩漏)——plumbing 零重複 ✓。分工:audit=量化蓋章(幾對、佔比、可匯出),
viz=逐對人工檢視(縮圖對照、確認是不是真重複)。
**建議(小文案)**:同 C 模式互指一句。

### E. 「coverage」一詞兩義

完整度熱力圖 = **embedding 空間**的稀疏/假完整(格子 / 嵌入盲區);
🩺 資料體檢「split 類別覆蓋」= **標註結構**的類別集合差(val 缺哪些類)。
兩者都會被口語稱為「覆蓋」。
**判定**:語義確實不同、都該存在;audit 節名已刻意叫「split 類別覆蓋」自我限定 ✓。
**建議(小文案)**:功能地圖在完整度那行補「(embedding 空間的覆蓋;split 類別覆蓋在 🩺)」。

### F. 體檢卡(單張)vs 資料體檢(資料集)—— 命名刻意呼應

資料體檢的 ❓ 已寫明兩者關係 ✓。
**殘留問題**:體檢卡的 S1 文案寫「來自**組考卷** / gauge study」,但組考卷工具目前
**隱藏中**(整併進 📥 標註回饋的規劃)——新使用者會找不到 S1 的來源入口
(手動輸入 slider 仍可用,但指路指向不存在的入口)。
**建議**:體檢卡 S1 文案補「(組考卷功能整併中,可先手動輸入)」——一行字的事。

### G. 「匯出」一詞重載(可接受,不動)

- 工具「匯出」= 策展購物車 → YOLO 子資料集(選影像的收斂點)
- 差異探索/資料體檢的「📦 匯出報告」= 產報告檔(HTML/CSV/JSON)
- Visualize 匯出清單 = CSV/ZIP

**判定**:字面重載但語境各自清楚(「匯出報告」vs「匯出子集」),功能地圖也有交代;
強行改名的攪動成本 > 混淆成本。**不動**。

### H. 既定分工重確認(維持,不重開)

- **找相似 ×3**(Visualize 以圖搜圖 / 瑕疵② 找同款 / 🎯 以樣搜樣):2026-07-06 盤點
  與 M12/M13 決策已刻意三分(單堆互動 / 掃描結果排序 / 跨資料集海撈),維持。
- **選樣目標同名不同義**(② balanced/novelty vs ③):已用 caption 揭露(4 句),維持;
  治本解=候選中的「S1/S2/S3 統一語言」輪。
- **瑕疵偵測 vs 差異探索**(單張 vs 群對群):M19 立案時就寫進雙方文案,已清楚。

## 2. 建議動作分級

| 級別 | 內容 | 成本 |
|---|---|---|
| **立即可做(維護輪,只動 app.py 文案)** | A/B/C/D/E/F 共 **7 句指路文案**:功能地圖「最常搞混」補第三對+完整度括號註;Compare/差異探索互指;旁證指 Compare;audit 離群/重複互指 viz;體檢卡 S1 補註 | ~30 分鐘+E2E 無涉(純文案) |
| **候選(另輪立案)** | S1/S2/S3 統一語言(把「同名不同義」「三種離群」收進同一套問題詞彙——治本) | 已在 M20 候選清單 |
| **不動(明確決策)** | 匯出重載、找相似三分、novelty 語義、PCA 旁證保留不擴建 | — |

## 3. 一句話總結

> 工具面沒有需要合併的重複——新加的兩個工具延續了「刻意分開」的既定紀律,
> 底層也正確共用了 plumbing;真正缺的是 **7 句互相指路的文案**:
> 使用者拿著「兩個資料夾」或想找「離群」時,畫面沒告訴他該進哪扇門。
